export default {
  testEnvironment: 'node',
  transform: {}, // disable babel transform if you don't use it
  testMatch: ['<rootDir>/public/ChessAI/test/**/*.js'], // <- use <rootDir> for root folder
};
